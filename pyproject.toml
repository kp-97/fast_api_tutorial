[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "fast_api_tutorial"
version = "0.1.0"

dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic"
]

[tool.setuptools]
packages = []